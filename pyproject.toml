[project]
name = "amp"
version = "0.0.5"
description = "AMP: is a fast admin dashboard template based on FastAPI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.0",
    "jinja2>=3.1.6",
    "uvicorn>=0.38.0",
]
